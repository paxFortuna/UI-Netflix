import 'package:flutter/material.dart';

class ContentsListScreen extends StatelessWidget {
  const ContentsListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Contents List'),
      ),
      body: Container(),
    );
  }
}
